<h1>Bienvenido {{name}}, tu correo es: {{email}}</h1>
<button onClick="location.href='/api/users/logout'">Logout</button>

<h1>Productos</h1>

<table>
    <thead>
        <tr>
            <th>Id</th>
            <th>Titulo</th>
            <th>Descripcion</th>
            <th>Precio</th>
        </tr>
    </thead>
    <tbody>
        {{#each products}}
        <tr>
            <td>{{_id}}</td>
            <td>{{title}}</td>
            <td>{{description}}</td>
            <td>$ {{price}}</td>
        </tr>
        {{/each}}
    </tbody>
</table>